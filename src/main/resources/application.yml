server:
  port: 9005

# Spring Boot 2.6+ 需配置此项，否则 Swagger 可能 404
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taian?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=GMT%2B8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
    settings:
      classic_compatible: true #处理空值
      datetime_format: yyy-MM-dd HH:mm
      number_format: 0.##
  mail:
    host: smtp.qq.com
    port: 587
    username: 772105823@qq.com
    password: iwaiklxicmkkbbgf
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
        socketFactoryClass: javax.net.ssl.SSLSocketFactory
    from: 772105823@qq.com
servlet:
  multipart:
    max-file-size: 10MB
    max-request-size: 100MB
mybatis-plus:
  type-aliases-package: com.taian.entity
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

# Swagger 文档
springdoc:
  api-docs:
    path: /v3/api-docs          # OpenAPI JSON 地址
  swagger-ui:
    path: /swagger-ui.html      # 文档页面地址
    tags-sorter: alpha          # 接口按字母排序
